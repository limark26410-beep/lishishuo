# 历史说 · 出片工具

## 启动（不用敲命令）

**三平台都有启动脚本**（自动建环境、检测 ffmpeg、启动服务）：

| 平台 | 脚本 | 说明 |
|---|---|---|
| macOS | 双击 `启动.command` | 首次被拦去 系统设置→隐私与安全性→仍要打开 |
| Windows | 双击 `启动.bat` | 首次运行需装好 Python（python.org 下载，勾选 Add to PATH） |
| Linux | `./start.sh` | 服务器运行，装好 python3-venv |

> 脚本会自动创建 venv、装依赖；检测 ffmpeg，缺失时打印本平台安装命令。

**还需要装一次 ffmpeg**（视频处理必需，装一次永久有效）：

| 平台 | 安装命令 |
|---|---|
| macOS | `brew install ffmpeg`（没 brew 先装：`/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"`） |
| Windows | `winget install ffmpeg`，或从 https://www.gyan.dev/ffmpeg/builds/ 下载 release 版，解压后把 bin 目录加进系统 PATH |
| Linux | `sudo apt install ffmpeg`（Debian/Ubuntu）/ `sudo yum install ffmpeg`（CentOS/RHEL） |

## 或者手动启动

```bash
cd ~/lishishuo
python3 web_app.py
```
浏览器打开 `http://127.0.0.1:8765`（端口被占会自动 +1，看终端打印的实际地址）。

---

## 用法

1. **期号** — 流水线工作目录名，如 `016`
2. **稿子路径** — 拖文件到输入框可直接得到路径，点「检查」会显示字数、是否有小标题
3. **图片来源** — 自动生图 / 本地目录（复用旧图，或放你自己拍的照片）
   > 自拍照片支持任意尺寸：横图、方图、竖图都会自动裁成竖屏，不变形
4. **片头标题** — 用 `·` 分隔，如 `唐朝·盛世气象`
5. **归档名** — 存进素材库用的名字，如 `16-唐朝`
6. 点「开始制作」

跑完自动归档到素材库，并在 `episodes/0XX/验收/` 生成四张截图。

---

## 视频混剪（新）

素材库规范（视频模式）：
```
~/历史说素材/视频/               ← 素材库根目录（网页里可改）
├── 历史/                       ← 题材（第一级目录）
│   ├── 隋唐/                   ← 主题（第二级目录，可选）
│   │   ├── 唐-长安城_01.mp4
│   │   └── README.txt          ← 每行「文件名：画面内容描述」，AI 选材用
│   └── 清朝/
├── 商品/
└── 美食/
```
- 单条素材 ≥ 15 秒；单个题材池总时长建议 ≥ 10 分钟；竖屏优先，1080p 以上最佳
- 网页里设置「视频素材库」根目录 + 选「本期题材」，开始制作即走视频混剪
  （素材按顺序轮播截取 → 1080x1920 竖屏 → 静音，只保留配音+BGM）
- 素材不足会明确报错「该题材缺素材」，不会静默

## 风格选择（新）

「风格」下拉两种预设，一键切换整套呈现参数（config.yaml `style.presets` 可改）：

| 风格 | 语速 | 转场 | 字幕 | 片头 | 适用 |
|---|---|---|---|---|---|
| **故事风格** | -4% 舒缓 | concat 硬切 | 60px | 3s | 长片纪录片（默认） |
| **短视频风格** | +100% 2倍速 | mixed 混合（高潮硬切+抒情溶解） | 66px | 2s | 1-3 分钟快节奏 |

- **手动语速优先**：在高级设置里手动改过语速 → 任何风格都不覆盖你的值；没改过才用风格预设
- 段落级配乐/节奏（可选）：config 里配 `tts.emotion_rates`、`video.ken_burns.emotions`、`bgm.library_root` 后自动按段落情绪配乐/变速/变语速，默认关闭

## 配乐素材库（可选，不配也能跑）

```
~/历史说素材/bgm/               ← 配乐库根目录（config bgm.library_root 可改）
├── 悬念/                       ← 开头钩子段用（低频铺垫、留白多，15~30s）
├── 平叙/                       ← 正文主体用（中速叙事感，60~120s）
├── 高潮/                       ← 转折/震撼段用（弦乐渐强、鼓点推进）
└── 收束/                       ← 结尾段用（舒缓收尾）
```
- 每情绪目录放 2~3 首无版权 mp3（文件名随意，取第一首）
- 缺素材/整库缺失 → 自动退回 `assets/bgm.mp3` 单曲兜底，不会中断
- 流水线自动判断每段情绪（AI 标注，失败降级关键词规则），不用手动选

## SDK：CLI 一条命令出片

```bash
cd ~/lishishuo
python3 -m lishishuo \
  --script 稿子.txt \
  --素材库 ~/历史说素材/视频/商品/ \
  --duration 3 \
  --output ./output/
```

- `--script` 稿子文件（必填）；`--素材库` 素材库目录（缺省用 config）；`--duration` 目标分钟（缺省按字数估）；`--output` 成片输出目录
- 成片 = 素材轮播 + 配音字幕片头，纯命令行，日志打 stdout，方便脚本捕获

## SDK：HTTP API

服务跑起来后（web_app.py），供同事程序化对接：

```bash
# 投稿子出片（script 支持：稿子文本 / 文件路径 / 投递目录路径）
curl -X POST http://127.0.0.1:8765/api/render \
  -H 'Content-Type: application/json' \
  -d '{"script":"这是稿子正文…","素材库":"/path/素材库/","title":"标题·副标题"}'
# → {"ok":true,"task_id":"029"}

# 查进度（轮询）
curl http://127.0.0.1:8765/api/task/029
# → {"status":"running","step":"拼接画面","progress":60,"logs":[...]}

# 下载成片
curl -o 成片.mp4 http://127.0.0.1:8765/api/output/029
```

响应带 CORS 头（`Access-Control-Allow-Origin: *`），浏览器/服务端都能调。

---

## 投递目录自动出片（外部工具对接）

网页「投递目录」卡片开启后，外部工具只需把稿子丢进目录，自动出片：

```
~/历史说素材/投递/
├── 历史/李白讲稿.txt      ← 外部丢这里（一级子目录名=题材，素材库自动选 视频/历史/）
├── xxx/                   ← 文件夹形态：内含 script.txt/稿子.txt/第一个 .txt
│   └── script.txt
├── _processing/           ← 处理中（自动移入，防重复触发）
├── _done/20260814/        ← 成功（按日期归档）
└── _failed/               ← 失败（原稿 + .err.txt 写明原因）
```

- 成片输出：`成片目录/{题材或空}/{稿子名}.mp4`（配置里的 out_dir）
- 丢根目录的 txt → 用默认素材库全库选材；丢 `题材/xxx.txt` → 只在该题材池内选材
- 题材素材库不存在 → 自动失败归档 `_failed` + 错误原因
- 处理中重复丢同名稿子 → 不重复触发

## 稿子格式（重要）

- **走文件/投递目录的稿子：第一行视为标题**（不参与配音和分段），正文从第二行开始。
  例：`李白·诗仙传奇` 作为首行标题，下面才是正文。
- **CLI/API 文本直传**（`--script` 传文本、`/api/render` 的 `script` 字段直接传稿子内容）：
  不走标题剥离，全文参与配音和分段，标题请通过 `--title`/`title` 字段单独传。
- AI 出稿模式：稿子由 AI 生成，无首行标题问题，标题走片头字段。

---

## 这一版做了什么

**补上了原流水线缺失的环节**（以前要人工做）：
- 片头 overlay（原 run.py 根本没有这一步）
- 主流程接上 v4 字幕方案（以前主流程用的还是旧 libass，参数对不上）
- 自动检查：超长行 / 全文比对 / 行数，不通过直接停
- 自动归档到素材库
- 自动生成验收截图

**参数全部收进 config.yaml**（以前写死在 .py 里）：
字幕位置 1000px、字号 60、片头 3 秒、CRF 26、TTS 声线语速、素材库路径。
界面「高级设置」里改，会写回 config.yaml。

---

## 两个人工卡点（不可省）

1. **稿子终审** — 史实、合规必须人工把关。程序看不出「GDP 占全球 50%」是网络神话、
   「驱除鞑虏」是合规雷。
2. **成片抽验**（约 1 分钟）：
   - 拖到片尾，字幕和声音对得上
   - 传手机用抖音发布预览，字幕没被 UI 挡住

其余全自动。

---

## 命令行用法（不想开界面时）

```bash
python3 run.py -e 016 \
  --title "唐朝·盛世气象" \
  --name "16-唐朝" \
  --skip-images          # 用 episodes/016/images/ 里已有的图
```

参数：
- `--title` 片头标题
- `--name` 归档名
- `--skip-tts` / `--skip-images` 跳过已完成的步骤
- `--no-archive` 不归档
- `--no-cleanup` 保留中间文件

---

## 目录约定

```
~/lishishuo/                 代码
  ├── web_app.py             界面服务
  ├── web_ui.html            界面页面
  ├── run.py                 流水线主程序
  ├── config.yaml            全部参数在这
  ├── lib/                   功能模块
  └── episodes/0XX/          工作目录（中间文件）

~/Desktop/历史说素材/         素材库（成品）
  ├── 成片/ 图片/ 录音/ 稿子/ 字幕/ 短视频/
```

---

## 排错

- **docx 读不了** → `pip3 install python-docx`（不装也有兜底，一般不会出问题）
- **字幕检查不通过** → 会打印具体哪几条超长/超行，改折行参数或稿子
- **片头没出现** → 检查是否填了「片头标题」，没填就不生成
- **端口被占** → 改 web_app.py 里的 `PORT = 8765`
