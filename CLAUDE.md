# lishishuo — 文史长音频自动化流水线 · 约束文件

## 画幅
- 9:16 竖屏 1080×1920，锁死

## 流程闸门
- 终审是必经闸门，不用未终审初稿出片
- 工具不自行更换，要换先报 Mark

## 生图
- 提供商：通义万相（key 存 ~/lishishuo/.env）
- 每期约 18-28 张，不是每段字幕一张
- 风格锚定词：中国古风、水墨质感、纪录片氛围、无文字

## 动效
- Ken Burns：先放大 2 倍再 zoompan，防糊边

## 配音
- edge-tts / zh-CN-YunjianNeural / rate=-4%
- **去标题**：喂 TTS 前删掉脚本第一行标题，只送正文（钩子开头）。标题仅用于片头画面。

## 编码
- 生产用 libx264（videotoolbox 是 macOS 独有，不上生产）
- 字幕烧录：h264+colorkey 黑底透明（PIL 渲染 PNG → h264 字幕轨 → colorkey 抠黑 → overlay）。不再用 libass/subtitles 滤镜。
- **TODO**: h264+colorkey 是当前方案，深色/纯黑场景可能抠出毛刺描边；待办：装含 libass 的 ffmpeg、改用 `-vf subtitles` 直烧替代。

## 片头与钩子（2026-07-22 锁定）
- 每期开头用终审稿里的开篇钩子，原样念。不改、不问、不念标题。
- 标题只做视觉片头（gen_title.py v3），不占旁白。

## 片头模板（第四期风格，固定 6 秒）
- gen_title.py v3
- 主标题：**大字 GOLD fs=69**（1-2 行，行距 25px），内容 = 朝代名 + 副标题
- 系列行：**小字 WHITE fs=36**，"上下五千年 · 第X期"，带 "·" 分隔
- 金线：400×3px GOLD，紧贴系列行下方（GLINE_GAP=18px）
- 整体垂直居中于 1920 画面
- **时长固定 6 秒，不可改**（改会破坏 −6s 字幕同步）

## 字幕（2026-07-22 锁定 v4）
- **真分辨率**：PlayResX=1080 / PlayResY=1920 / FontSize=60（真像素）/ MarginV=140
- **不再用 FontSize=9 的 384 空间**
- SRT 从 edge-tts VTT 生成（真实音频时间轴，非估算）
- 烧录方式：PIL 渲染黑底白字 PNG（固定 BAR_HEIGHT=300px） → h264_videotoolbox 字幕视频 → colorkey 抠黑 → overlay@y=1480
- 折行硬规矩：
  * 每行 ≤14 字
  * 每条 ≤2 行
  * 长条目（>28 字）按标点拆分为多个 SRT 条目，时间按字数比例分配
  * 折行后全文比对配音脚本——逐字一致，零丢字
- 字幕整体在画面内、底部留白 140px、两行不出框

## 同步（2026-07-22 锁定）
- SRT 烧录前先 **−6 秒**（片头时长），或在正文视频上烧好字幕后再 concat 片头
- 片头固定 6 秒，SRT 平移量 = 片头时长

## 成片拼接
- concat 片头（无音频）+ 正文（有音频、已烧字幕）
- filter_complex: `[0:v][1:v]concat=n=2:v=1:a=0[v]` + map audio from body

## 每期自检清单（跟成片一起交，缺一不可）
1. 超长行扫描 = 0（>14 字的行）
2. **全文比对 = 一致**（折行后拼接所有字幕文字，跟配音脚本逐字比对，无掉条）
3. 每条 ≤2 行 + 整条在画面内 + 底部留白 + 不顶出框
4. 三张对轴截图：0:30 / 中段 / 片尾——屏上字=当下念的词
5. 无双重字幕/残影

## 备份铁律
- 每次覆盖成片前，先备份上一版好成片（`*_GOOD_*.mp4`）
- 改片头前先备，万一改坏能回滚

## API Key
- 只进 ~/lishishuo/.env 环境变量，不进仓库

## 成片归档
- ~/Desktop/历史说/成片/{期号}-{标题}/
- 素材归档 ~/Desktop/历史说素材/（成片/图片/录音/稿子 四类）
