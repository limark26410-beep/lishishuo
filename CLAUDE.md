# lishishuo — 文史长音频自动化流水线 · 约束文件

## 画幅
- 9:16 竖屏 1080×1920，锁死

## 流程闸门
- 终审是必经闸门，不用未终审初稿出片
- 工具不自行更换，要换先报 Mark

## 生图
- 提供商：通义万相（key 存 ~/lishishuo/.env）
- 每期约 18-28 张，不是每段字幕一张
- 风格锚定词：中国古风、水墨质感、纪录片氛围、无文字

## 动效
- Ken Burns：先放大 2 倍再 zoompan，防糊边

## 配音
- edge-tts / zh-CN-YunjianNeural / rate=-4%
- **去标题**：喂 TTS 前删掉脚本第一行标题，只送正文（钩子开头）。标题仅用于片头画面。

## 编码
- 生产用 libx264（videotoolbox 是 macOS 独有，不上生产）
- 最终导出：libx264 CRF 26 preset medium（每期成片二次压，≈330MB/24min）
- 字幕烧录：h264+colorkey 黑底透明（PIL 渲染 PNG → h264 字幕轨 → colorkey 抠黑 → overlay）。不再用 libass/subtitles 滤镜。
- **TODO**: h264+colorkey 是当前方案，深色/纯黑场景可能抠出毛刺描边；待办：装含 libass 的 ffmpeg、改用 `-vf subtitles` 直烧替代。

## 片头与钩子（2026-07-23 锁定 v5）
- 每期开头用终审稿里的开篇钩子，原样念。不改、不问、不念标题。
- 标题只做视觉片头（gen_title.py v3），不占旁白。
- **钩子必须从 0:00 就响**：抖音前几秒静音会大量划走，开头不能空。

## 片头模板（第四期风格，固定 6 秒）
- gen_title.py v3
- 主标题：**大字 GOLD fs=69**（1-2 行，行距 25px），内容 = 朝代名 + 副标题
- 系列行：**小字 WHITE fs=36**，"上下五千年 · 第X期"，带 "·" 分隔
- 金线：400×3px GOLD，紧贴系列行下方（GLINE_GAP=18px）
- 整体垂直居中于 1920 画面
- **时长固定 6 秒，不可改**

## 片头叠加方式（2026-07-23 锁定，改掉旧 concat 方案）
- **片头以 overlay 叠加方式盖在正文前 6 秒，不用 concat 拼接**
- 音频始终从 0:00 开始，**不得使用 adelay 或任何延迟**
- 前 6 秒画面 = 标题卡覆盖正文，同时配音正常播放（钩子 0:00 就响）
- 6 秒后画面转入正文，声音全程连续不断
- 成片总时长 = 正文时长（不是正文 + 6 秒）
- 因为正文视频没被推后，字幕不需要 −6 秒平移——字幕与音频从 0:00 同步走
- FFmpeg 命令：`overlay=0:0:enable='between(t,0,6)':eof_action=pass`

## 字幕（2026-07-22 锁定 v4）
- **真分辨率**：PlayResX=1080 / PlayResY=1920 / FontSize=60（真像素）/ MarginV=280
- **不再用 FontSize=9 的 384 空间**
- SRT 从 edge-tts VTT 生成（真实音频时间轴，非估算）
- 烧录方式：PIL 渲染黑底白字 PNG（固定 BAR_HEIGHT=300px） → h264 字幕视频 → colorkey 抠黑 → overlay
- 折行硬规矩：
  * 每行 ≤14 字
  * 每条 ≤2 行
  * 长条目（>28 字）按标点拆分为多个 SRT 条目，时间按字数比例分配
  * 折行后全文比对配音脚本——逐字一致，零丢字
- 字幕整体在画面内、底部留白充足、两行不出框

## 字幕位置验收（2026-07-23 锁定）
- **以实际像素为准，不以参数为准**——MarginV 经 bar 高度、内部偏移换算后实际落点可能不符预期
- 验收标准：**字幕底部距画面底边约 300px（实测）**，不是"MarginV 参数写了 200"
- 如实测不符，直接调整最终 overlay y 坐标，不纠结中间换算公式

## 同步（2026-07-23 锁定）
- 片头以 overlay 覆盖，正文视频不被推后
- 字幕和音频从 0:00 同步走，**不需要 −6 秒平移**

## 成片拼接
- overlay 片头覆盖正文前 6 秒（不是 concat）
- 正文（有音频、已烧字幕）→ overlay 前 6 秒覆盖标题卡 → 最终视频

## 每期自检清单（跟成片一起交，缺一不可）
1. 超长行扫描 = 0（>14 字的行）
2. **全文比对 = 一致**（折行后拼接所有字幕文字，跟配音脚本逐字比对，无掉条）
3. 每条 ≤2 行 + 整条在画面内 + 底部留白 + 不顶出框
4. 对轴截图（验同步）：三张——0:30 / 中段 / 片尾，屏上字 = 当下念的词
5. 单行 / 双行字幕位置图（验位置）：字幕底部距底边实测 ≈300px
6. 0:00 开头截图 + 确认 0:00 有人声（钩子没被埋）
7. 无双重字幕 / 残影

**⚠️ 自检铁律（2026-07-23）**：程序自检只能验证"参数是否写对"，不能验证"画面上是否真的对"。第12期五项自检全过但实际同步和位置都不对。
每期必须提交实拍截图，以截图为准验收。参数计算结果不可作为"位置正确"或"同步正确"的证据。

## 备份铁律
- 每次覆盖成片前，先备份上一版好成片（`*_GOOD_*.mp4`）
- 改片头前先备，万一改坏能回滚

## API Key
- 只进 ~/lishishuo/.env 环境变量，不进仓库

## 成片归档
- ~/Desktop/历史说/成片/{期号}-{标题}/
- 素材归档 ~/Desktop/历史说素材/（成片/图片/录音/稿子 四类）
